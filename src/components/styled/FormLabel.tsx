import type { ComponentStyleConfig } from "@chakra-ui/theme";

export const FormLabel: ComponentStyleConfig = {
  baseStyle: {
    fontWeight: "normal",
    color: "gray.400",
    fontSize: "sm",
  },
};
